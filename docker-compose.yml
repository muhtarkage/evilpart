version: "3.9"
services:
  app:
    build: .
    command: npm start
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    depends_on: []
      
      


volumes:
  mongo_data:
